# AGENTS.md — Berichtsheft

## What this is
Single-container app. Scrapes "Lehrstoff" (teaching content) from WebUntis JSON/REST API and serves a small week-based viewer + auto-generates copy-paste Berichtsheft text. Runs amd64 + arm64 (Pi).

## Layout
- `app/scraper.py` — orchestration only. `scrape_week(week_id, settings=None)` ties together the HTTP client, pure date helpers, and encrypted storage below to produce and persist one ISO week's lesson data via `storage.save_week_data(settings.user_id, ...)`; re-exports names from the split modules for backward compat (tests/`main.py` still reach them as `scraper.X`). Consecutive lessons merge on `(date, subject, content)` — same day, same subject, identical teaching content — not on start time.
  - **Week-status detection**: when a week has no usable (non-cancelled) lessons, `scrape_week()` tries to explain why instead of silently doing nothing, and persists a `{"days": [], ...}` placeholder with one of four flags (see Gotchas below for the exact flag names/priority). This is the one situation where an empty-lessons week still gets written, guarded by `_has_real_lessons(user_id, week_id)` (real lesson data is never overwritten by a placeholder) — every other path only saves when there's real lesson data.
- `app/config.py` — runtime config read from the environment. Live vars: `SECRET_ENCRYPTION_KEY`, `ADMIN_USERNAME`, `ADMIN_PASSWORD`, `DATA_DIR`, `DEBUG_DUMPS`. `UNTIS_HOST`/`SCHOOL`/`USER`/`PASS` and `IHK_*`/`SCRAPE_DAY`/`SCRAPE_TIME` are legacy/test-only fallback (read only via each function's `settings or UserSettings.from_config()` default — no route in `main.py` ever calls with `settings=None`, so these only fire in tests). No `SUBJECT_FILTER` in the current codebase — an earlier version had one, never carried over, not read anywhere. Standalone leaf module, no dependents of its own; other modules do `import config` and read `config.X` at call time (not `from config import X`) so `monkeypatch.setattr(config, "UNTIS_USER", ...)` in tests reaches the code that uses it.
- `app/untis_client.py` — `UntisClient` + `ScrapeError`. Login flow: JSON-RPC `authenticate` → session cookie + personId → `/WebUntis/api/token/new` → bearer token → JSON-RPC `getTimetable` → per-lesson REST `calendar-entry/detail` for `teachingContent`. Debug dumps to `data/debug/` on unexpected API shapes (via `storage.py`).
- `app/time_utils.py` — pure time/week-id helpers: `_hm`, `week_bounds`, `current_week_id`.
- `app/school_calendar.py` — pure holiday/school-year gap detection: `_is_full_holiday_week`, `_is_between_school_years`. No hardcoded dates, derived entirely from WebUntis's own data.
- `app/storage.py` — per-user encrypted CRUD over SQLite (via `db.py`'s `*_row` functions + each user's DEK, see `crypto.py`), never touches a filesystem path for real data. `_has_real_lessons(user_id, week_id)` guards every placeholder write — a previous placeholder guess is always safe to overwrite with a better-informed one later (e.g. once `getSchoolyears` data becomes available), but real saved lessons are never overwritten by a placeholder. `_dump_debug(name, payload, user_id=None)` is the one exception to "no filesystem": dev-only, gated by `config.DEBUG_DUMPS`, writes **plaintext unencrypted** raw API dumps to `{DATA_DIR}/{user_id}/debug/` — called from 7 sites in `untis_client.py` on unexpected API shapes.
- `app/ihk_client.py` — `IhkClient` + `IhkError`. Plain HTTP against the IHK tibrosBB portal (classic JSP/Tomcat, not a REST API). `login()`/`logout()`, `list_entries()` (parses `azubiHeft.jsp`'s week list into `{week_id: {lfdnr, status}}` — the *only* source of truth for which weeks exist/are locked), `create_next_entry()` (POSTs "Neuer Eintrag" — returns an unpersisted DRAFT dict, `lfdnr='0'`), `fetch_entry(lfdnr)`, `save_entry(entry, ausbinhalt3, ausbinhalt1=None, ausbinhalt2=None)` (always re-fetches after POSTing to independently verify the save actually persisted, never trusts the response — see Gotchas). `_parse_entry_form()` is the shared BeautifulSoup field-extraction helper for both an existing entry and a draft.
- `app/ihk_submitter.py` — IHK orchestration, separate from `scraper.py` (different external system, no shared logic). `submit_week(week_id, formatted_text, ausbinhalt1=None, ausbinhalt2=None)` finds-or-creates the right entry and saves into it; refuses a `genehmigt` (locked) week and refuses to skip ahead of the next sequential week. `sync_status()` refreshes `data/ihk_status.json` (status/lfdnr metadata only — deliberately never fetches content, see Gotchas); `load_status()` reads it back, `{}` if never synced.
- `app/main.py` — FastAPI app, multi-user. Routes (21 total, all `Depends(auth.require_user)` unless noted): auth (`POST /api/auth/login` public, `POST /api/auth/logout`, `GET /api/auth/whoami`), admin-only (`POST`/`GET /api/admin/users`), settings (`GET`/`PUT /api/me/settings`, `PUT /api/me/password`, `POST /api/test/untis`, `POST /api/test/ihk`), weeks (`GET /api/weeks`, `GET /api/weeks/{week_id}`, `POST /api/scrape`), IHK (`GET /api/ihk-status`, `GET /api/ihk-history`, `GET /api/ihk-entry/{week_id}` — live read-only pre-fill fetch, `POST /api/submit-ihk`), bulk ops (`POST /api/bulkops/scrape-weeks`, `GET /api/bulkops/scrape-progress`, `POST /api/bulkops/backfill-ihk`, `POST /api/bulkops/scrape-cancel`). Full request/response shapes in DEVELOPER.md. Background thread `scheduler()` does per-user auto-scrape (each user's own `scrape_day`/`scrape_time` settings row, default Sunday 18:00 container TZ), scraping **current + previous ISO week** each run (`_weeks_to_scrape()`/`_scheduled_scrape(user_id, today=None)`) — catches Lehrstoff teachers add late for the week that just ended. Each week is scraped independently; one failing doesn't block the other. Due-check logic (`_scrape_due(now, last_run_date, scrape_day, scrape_hour, scrape_minute)`, pure function, no module globals) is unit tested in `tests/test_api.py` rather than only verified by waiting for a real Sunday — but its "already ran today" tracking (`last_run` dict in `scheduler()`) is in-memory only, resets on restart. IHK status sync runs best-effort after every scrape (manual and scheduled) and every submit — a sync failure never breaks the response it's piggybacked on (both call sites wrap it in `except Exception`, not just `IhkError`). Locking is per-user: `_get_lock(user_id, kind)` where `kind` is `"scrape"` or `"submit"` — replaced the old single global `scrape_lock`/`submit_lock` pair. Serves `static/` at `/`. **Security hardening (2026-08, see wiki `berichtsheft-security-audit-2026-08`):** login rate-limiting (429 after 8 fails/5 min per ip+username), password policy (min 8 chars), PBKDF2 600k with transparent rehash-on-login, timing-safe dummy-verify for unknown users, all-sessions invalidation on password change, a `Secure`-cookie flag (`SESSION_COOKIE_SECURE`), a security-headers middleware (CSP/X-Frame-Options/nosniff/Referrer-Policy), API docs gated off by default (`ENABLE_DOCS`), SSRF host validation in `app/netcheck.py` (used by both external clients), a `MAX_BULK_WEEKS=520` cap + abort (`scrape-cancel`) on bulk scrape, and sanitized 500 responses (no raw exception text).
- `static/index.html` — the whole authed UI is now **one single-page shell**, no build step, no framework. A persistent sidebar (Wochen / Datenimport / Einstellungen / Hilfe + logout) plus a hash router swap four hidden `<section class="view">`s (`#/week` default, `#/import`, `#/settings`, `#/help`) — navigation never reloads the page. On mobile (≤768px) the sidebar is an off-canvas drawer toggled by a `☰` top bar + backdrop. All styling lives in one shared stylesheet `static/app.css` (view-specific rules scoped under `#view-week`/`#view-import`/`#view-settings`/`#view-help` so identically-named classes can't collide). Logic is split into ES modules under `static/js/`:
  - `js/app.js` — shell: exports `$`, `authFetch` (fetch wrapper that bounces to `/login.html` on 401), and `ready` (a promise resolving to the current user; views `await` it before any network call so the single auth guard runs first). Owns the cache-clearing `logout()`, `ensureCacheOwnership()`, the mobile drawer, the hash router (`showRoute()` toggles `.active`), and removes `.admin-only` nodes for non-admins.
  - `js/week.js` — the week viewer (the old inline script). `renderWeek()` builds one merged card: two writable textareas (`ausbinhalt1`/`ausbinhalt2`, always blank on load — one-way flow, never pre-filled from IHK) + the read-only "Berufsschule" text. The IHK button is tri-state (`enabled`/`disabled` with a `title` reason/`hidden`), computed from `ihkStatus` + `nextSubmittableWeek()`; also force-hidden for any week after `currentWeek`. `IHK_BADGE_LABELS` maps the 4 known statuses to German label + CSS class.
  - `js/settings.js` — settings as **one flat grouped page** (Konto → WebUntis → Berichtsheft[start date + auto-scrape schedule together] → IHK → Administration, admin-only), no tabs. Handlers wired via `addEventListener` (module scope, not global). Same endpoints as before (`/api/me/settings`, `/api/me/password`, `/api/test/{untis,ihk}`, `/api/admin/users`).
  - `js/bulkops.js` — the Datenimport view (bulk week scrape + IHK backfill). Its scrape button id is `bulkScrapeBtn` (renamed to avoid colliding with the week view's `scrapeBtn`).
  - The Hilfe view is static markup inside `index.html` (describes the new UI). `static/settings.html`, `help.html`, `bulkops.html` are now 3-line redirect stubs to their hashes (kept so old bookmarks work). `static/logo.svg` is gitignored/user-local; the sidebar `<img>` hides itself via `onerror` if absent.
- `data/app.db` — single SQLite file, all real data (users, sessions, per-user settings, week data, IHK history/status/local-fields, wrapped DEKs), see DEVELOPER.md's schema table for the 9 tables. No per-week JSON files anymore.
- `data/<user_id>/debug/` — raw, **unencrypted** API dumps, only written when `config.DEBUG_DUMPS` is truthy (dev-only, off by default).
- `compose.yaml` — single service, builds from `Dockerfile`, binds `0.0.0.0:8001:8000` (reachable on the network, not localhost-only), `.env` for secrets, bind-mounts `./data:/data`.
- `Dockerfile` — python:3.12-slim, installs `requirements.txt` (not `requirements-dev.txt` — pytest isn't in the built image by default), copies `app/` + `static/` + `tests/` + `pytest.ini`, runs uvicorn on 8000 internal.
- `.env` (gitignored, real secrets) / `.env.example` (template) — live/load-bearing: `SECRET_ENCRYPTION_KEY`, `ADMIN_USERNAME`, `ADMIN_PASSWORD`. Also present but legacy/test-only (see `config.py` bullet above): `UNTIS_USER`, `UNTIS_PASS`, `UNTIS_HOST`, `UNTIS_SCHOOL`, `SCRAPE_DAY`, `SCRAPE_TIME`, `IHK_USER`, `IHK_PASS`, `IHK_HOST`, `IHK_AUSBABSCHNITT`, `IHK_AUSB_MAIL`. Note: `.env.example`'s comment above `ADMIN_USERNAME`/`ADMIN_PASSWORD` says bootstrap triggers "if UNTIS_USER is set" — that's wrong, the actual gate in `db.bootstrap_admin_if_needed()` is `ADMIN_PASSWORD` being set, unrelated to `UNTIS_USER`.

## Conventions
- No linter config. Keep it that way unless asked — this is a small personal-use tool, don't over-engineer.
- **Tests are required for further changes to pass.** `tests/test_scraper.py` (39 tests) covers pure scraper logic (`_hm`, `week_bounds`, `current_week_id`, merging/filtering in `scrape_week`, credential check) and all four empty-week flags (`unavailable`/`holiday`/`schoolYearBoundary`/`allCancelled`, including priority order and the `_has_real_lessons` overwrite guard) with WebUntis network calls stubbed via monkeypatch. `tests/test_api.py` (38 tests) covers all five routes (`/api/weeks`, `/api/weeks/{id}`, `/api/scrape`, `/api/ihk-status`, `/api/submit-ihk`) via FastAPI `TestClient`, with `scraper.scrape_week`/`ihk_submitter.submit_week`/`sync_status` monkeypatched so no real network calls happen, plus auth/multi-user-isolation cases (401 with no session, 403 for non-admin admin-endpoint calls, cross-user week-data isolation). `tests/test_ihk_submitter.py` (18 tests) covers `IhkClient`/`ihk_submitter` at the method-stub level (`fake_ihk` fixture mirrors `fake_untis`) — mostly regression tests for real bugs hit live against the portal (silent-save, field-clobber, draft/lfdnr diff), see Gotchas. `tests/test_security.py` covers the 2026-08 hardening (SSRF host guard, password policy, login rate-limit, session invalidation on password change, bulk-scrape cap + abort, security headers, docs gating). 123 tests total. All tests authenticate through the real HTTP auth flow (`conftest.py`'s `new_user`/`admin_client`/`user_settings` fixtures create/log in real users via `POST /api/admin/users` + `POST /api/auth/login`) — no test-only endpoints or DB-bypass fixtures. Run any change through this suite before considering it done, and add cases for new behavior.
- Week id format everywhere: `YYYY-Www` (e.g. `2026-W29`), validated via `WEEK_RE = r"^\d{4}-W\d{2}$"` in both scraper and API layer — keep both in sync if changed.
- Errors from WebUntis surface as `scraper.ScrapeError`; `main.py` maps that to HTTP 502, unexpected exceptions to 500. Errors from the IHK portal surface as `ihk_client.IhkError`, mapped the same way.
- Debug dumps (`_dump_debug`) are the primary diagnostic tool when WebUntis changes its API shape — check `data/debug/` before assuming code bugs on scrape failures.

## Running / verifying changes
```bash
cd /Users/tim/claude/BetterAutoBerichtsheft2
docker compose up -d --build
curl -s http://127.0.0.1:8001/api/weeks
```
No `.env` secrets should ever be committed — already gitignored, double check before any commit touching it.

## Running tests
Tests run inside the real project container (not an ad-hoc image), so they exercise the exact same base image/deps as prod:
```bash
docker compose build
docker run --rm \
  -v "$PWD/tests":/srv/tests \
  -v "$PWD/pytest.ini":/srv/pytest.ini \
  --entrypoint bash \
  betterautoberichtsheft2-berichtsheft \
  -c "pip install -q pytest==8.3.4 httpx==0.28.1 && cd /srv && pytest -q"
```
`requirements-dev.txt` documents the pinned dev deps (pytest, httpx) if you want a persistent test image/venv instead of installing ad-hoc. `pytest.ini` sets `testpaths = tests`; `tests/conftest.py` sets `DATA_DIR`/`SECRET_ENCRYPTION_KEY`/`ADMIN_USERNAME`/`ADMIN_PASSWORD`/`SCRAPE_DAY=off` env vars to isolated test values before `app.main` is ever imported (required since `app/db.py` computes its SQLite path from `DATA_DIR` once at import time), then exposes `new_user`/`admin_client`/`user_settings`/`unauth_client` fixtures for real-endpoint test-account setup.

## Gotchas
- WebUntis bearer token endpoint (`/WebUntis/api/token/new`) can silently fail (non-200 or oversized body) — code already guards this (`teaching_content` returns `""` if no token), don't remove that guard.
- Port is bound to `0.0.0.0:8001` on host (see `compose.yaml`) — reachable from other machines on the network, not localhost-only. Intentional, not a bug, don't "fix" without asking.
- **Four distinct empty-week flags**, checked in this priority order in `scrape_week()` (each shown as its own placeholder in `static/index.html`'s `renderWeek()`):
  1. `"unavailable": true` — `getTimetable` error `-7004` ("no allowed date"): the week is beyond WebUntis's publish horizon (too far in the future). Not fixable now, no further checks attempted, just surfaced plainly. Re-scraping later will pick up real data once WebUntis opens that window.
  2. `"holiday": true` — confirmed via `getHolidays` (`UntisClient.holidays()` + `_is_full_holiday_week()`), or as a second attempt via `getSchoolyears` (`UntisClient.school_years()` + `_is_between_school_years()`, no hardcoded dates — derived entirely from WebUntis's own school-year boundaries) when the week sits in the gap between two school years. Either path shows plain "Schulferien".
  3. `"schoolYearBoundary": true` — `getTimetable` error `-8507` ("start/end not within a single school year") fired, but neither `getHolidays` nor `getSchoolyears` could confirm it either way (both can themselves fail right at the exact boundary moment — observed live as error `-8998`, "sy is null"). Last-resort honest guess, shown as "Wahrscheinlich Schuljahrwechsel".
  4. `"allCancelled": true` — `getTimetable` succeeded and returned real periods, but every one had `code: "cancelled"` (e.g. first week of a new school year before the real schedule is active) and it's not a confirmed holiday/gap either. Unlike the other three flags, this one still flows through `renderWeek()`'s normal text/submit path (`noSchedule` in the frontend) rather than a dead-end placeholder — the Berufsschule box just shows "Alle Stunden abgesagt" and stays submittable. Real case hit live: 2026-W36, the literal first day of the 2026/2027 school year.
  - If none of these apply (e.g. `getTimetable` returns a genuinely empty list with no explanation available), nothing is saved, so a later scrape can pick it up once WebUntis has more to say about it.

## IHK submission — key mechanics

`app/ihk_client.py`'s module docstring has the full debugging story; the load-bearing points:

- **Writes flow one way only: this app → IHK, never the reverse.** The app only ever *writes* to IHK via an explicit submit; it never modifies the portal on its own. **Reads** are now allowed for display: `ihk_submitter.fetch_week_fields(week_id, settings)` does a live READ-ONLY fetch of one week's `ausbinhalt1/2` (`list_entries()` → `fetch_entry(lfdnr)`, same week keying as `submit_week`), surfaced via `GET /api/ihk-entry/{week_id}` (best-effort: any failure → `null`, never an error). `static/js/week.js`'s `prefillFromIhk()` calls it **only after a manual 'Jetzt Abrufen'** and fills the two boxes so the user can update existing content instead of blind-overwriting it — guarded so it never clobbers text the user already typed and bails if the week changed mid-fetch. This pre-fill path was previously built, reverted, and is now **deliberately reintroduced** (user request, read-for-display only). `sync_status()` still fetches status/lfdnr metadata only — don't make it fetch content. The submit safety net still holds: empty boxes send `null`, so `save_entry` preserves whatever is already on the portal.
- **Never click "Speichern & Senden" (`sent`) automatically** — only ever `save`. Sending for approval is a one-way step toward the locked `genehmigt` state and must stay a manual action on the real site.
- **`<button>` elements with no `value=` attribute submit an empty string**, not their label text, when a real browser clicks them (`save`, `sent`, `neu` all lack `value=`). Check this pattern before wiring any new IHK form field.
- **"Neuer Eintrag" does not persist anything by itself.** It returns a blank DRAFT (`lfdnr='0'`) for what will become the next sequential week; the portal only assigns a real `lfdnr` once that draft is actually saved. `save_entry()` diffs `list_entries()` before/after the *save* POST (not around "Neuer Eintrag") to discover the assigned `lfdnr` — the opposite ordering caused a real production bug (`"expected exactly one new entry ... got set()"`, 2026-W30).
- **`ausbinhalt13` is an optimistic-concurrency field**, not a static placeholder — it mirrors `ausbinhalt3`'s current saved value. A stale one gets silently rejected (HTTP 200, text echoed back, nothing persisted) — always re-scrape it fresh immediately before every save, same as `token`.
- **`save_entry()` defaults `ausbinhalt1`/`ausbinhalt2` to the entry's current value** when the caller passes `None` — never hardcode `""`, that silently wipes manually-entered content on the real site (a real bug, now regression-tested).
- **Never trust a POST response as proof of success** — `save_entry()` always independently re-fetches and compares afterward.
- Only two Berichtsheft-relevant statuses were seen live for a long time (`Nachweis genehmigt`→`genehmigt`, `in Bearbeitung bei Azubi`→`in_bearbeitung`); `Warten auf Genehmigung`→`warten_auf_genehmigung` was confirmed live 2026-08-05 (2026-W29, after a real "Speichern & Senden" on the portal). `abgelehnt` is still an unconfirmed guess at the portal's own filter-checkbox label.
